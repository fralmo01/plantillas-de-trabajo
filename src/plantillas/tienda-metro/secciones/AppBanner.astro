<section class="tm-app">
  <div class="tm-wrap tm-app__inner">
    <div class="tm-app__tx">
      <p class="tm-app__kicker">Conoce los beneficios de nuestra</p>
      <h2>APP tienda.com</h2>
      <div class="tm-app__badges">
        <span class="tm-app__badge">Disponible en App Store</span>
        <span class="tm-app__badge">Disponible en Google Play</span>
      </div>
      <button type="button" class="tm-btn tm-btn--claro">Descargala aqui</button>
    </div>
    <img src="https://picsum.photos/seed/tm-app/420/320" alt="App tienda.com Market" loading="lazy" />
  </div>
</section>

<style>
  .tm-app {
    background: linear-gradient(135deg, var(--tm-amarillo) 0%, var(--tm-amarillo-osc) 100%);
  }

  .tm-app__inner {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 2rem;
    padding-block: 2.5rem;
  }

  .tm-app__kicker {
    font-size: 0.85rem;
    font-weight: 600;
  }

  .tm-app__tx h2 {
    font-size: clamp(1.5rem, 3.5vw, 2.2rem);
    color: var(--tm-rojo);
    margin: 0.25rem 0 1rem;
  }

  .tm-app__badges {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
    flex-wrap: wrap;
  }

  .tm-app__badge {
    background: #111;
    color: #fff;
    font-size: 0.72rem;
    font-weight: 700;
    padding: 0.5rem 0.9rem;
    border-radius: 8px;
  }

  .tm-app__inner img {
    border-radius: 16px;
    max-width: 260px;
  }

  @media (max-width: 700px) {
    .tm-app__inner {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .tm-app__badges {
      justify-content: center;
    }
    .tm-app__inner img {
      margin-inline: auto;
    }
  }
</style>
