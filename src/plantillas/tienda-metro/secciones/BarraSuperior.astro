<div class="tm-top">
  <div class="tm-wrap tm-top__inner">
    <span>Como quieres recibir tu pedido?</span>
    <button type="button" class="tm-top__chip">Elegir</button>
  </div>
</div>

<style>
  .tm-top {
    background: #fff;
    border-bottom: 1px solid var(--tm-linea);
    font-size: 0.82rem;
  }

  .tm-top__inner {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding-block: 0.55rem;
  }

  .tm-top__chip {
    border: 1.5px solid var(--tm-rojo);
    color: var(--tm-rojo);
    font-weight: 700;
    font-size: 0.78rem;
    padding: 0.25rem 0.9rem;
    border-radius: 6px;
  }

  .tm-top__chip:hover {
    background: var(--tm-rojo);
    color: #fff;
  }
</style>
