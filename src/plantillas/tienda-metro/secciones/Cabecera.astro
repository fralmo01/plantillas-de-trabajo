---
import { marca, menuCategorias } from '../datos/metro.js';

const base = '/plantillas/tienda-metro';
---

<header class="tm-hdr" id="top">
  <div class="tm-wrap tm-hdr__row">
    <a href={`${base}/`} class="tm-logo" aria-label={`${marca.nombre} ${marca.sub}`}>
      <span class="tm-logo__punto" aria-hidden="true"></span>
      <span class="tm-logo__tx">{marca.nombre}<b>{marca.sub}</b></span>
    </a>

    <div class="tm-menu">
      <button type="button" class="tm-menu__btn" id="tm-menu-btn" aria-expanded="false" aria-controls="tm-mega">
        Categorias
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4"><path d="M6 9l6 6 6-6"></path></svg>
      </button>

      <div class="tm-mega" id="tm-mega" hidden>
        <ul class="tm-mega__cats">
          {menuCategorias.map((m: { cat: string; sub: string[] }, i: number) => (
            <li>
              <button type="button" class={`tm-mega__cat${i === 0 ? ' is-on' : ''}`} data-cat={m.cat}>
                <span class="tm-mega__ico" aria-hidden="true">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6h15l-1.5 9h-12z"></path><circle cx="9" cy="20" r="1.4"></circle><circle cx="17" cy="20" r="1.4"></circle></svg>
                </span>
                {m.cat}
              </button>
            </li>
          ))}
        </ul>
        <div class="tm-mega__panel">
          {menuCategorias.map((m: { cat: string; sub: string[] }, i: number) => (
            <div class="tm-mega__sub" data-for={m.cat} hidden={i !== 0}>
              <h3>{m.cat}</h3>
              <ul>
                {m.sub.map((s: string) => <li><a href={`${base}/buscar/?q=${encodeURIComponent(s.toLowerCase())}`}>{s}</a></li>)}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </div>

    <form class="tm-buscar" id="tm-buscar" role="search" action={`${base}/buscar/`} method="get">
      <input type="search" name="q" id="tm-buscar-input" placeholder="Que buscas hoy?" aria-label="Buscar productos" autocomplete="off" />
      <button type="submit" aria-label="Buscar">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
          <circle cx="11" cy="11" r="7"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </button>
    </form>

    <div class="tm-hdr__acc">
      <a href={`${base}/#tabs`} class="tm-hdr__ico" aria-label="Mis listas">
        <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 3h12v18l-6-3-6 3z"></path></svg>
        <span>Mis listas</span>
      </a>
      <a href={`${base}/#tabs`} class="tm-hdr__ico" aria-label="Rastrea tu pedido">
        <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 7h13v10H3zM16 10h4l1 3v4h-5"></path><circle cx="7" cy="18" r="1.6"></circle><circle cx="17.5" cy="18" r="1.6"></circle></svg>
        <span>Rastrea tu pedido</span>
      </a>
      <button type="button" class="tm-hdr__ico tm-hdr__cart" id="tm-cart" aria-label="Ver carrito">
        <svg width="21" height="21" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6h15l-1.5 9h-12z"></path><path d="M6 6 5 3H2"></path><circle cx="9" cy="20" r="1.6"></circle><circle cx="18" cy="20" r="1.6"></circle></svg>
        <span>Carrito</span>
        <span class="tm-hdr__badge" id="tm-cart-count" hidden>0</span>
      </button>
    </div>
  </div>
</header>

<script>
  const btn = document.getElementById('tm-menu-btn');
  const mega = document.getElementById('tm-mega');
  const cats = Array.from(document.querySelectorAll<HTMLButtonElement>('.tm-mega__cat'));
  const subs = Array.from(document.querySelectorAll<HTMLElement>('.tm-mega__sub'));

  function abrir(v: boolean) {
    if (!mega || !btn) return;
    mega.hidden = !v;
    btn.classList.toggle('is-active', v);
    btn.setAttribute('aria-expanded', String(v));
  }

  btn?.addEventListener('click', (e) => {
    e.stopPropagation();
    abrir(mega?.hidden ?? true);
  });

  cats.forEach((c) => {
    const activar = () => {
      cats.forEach((x) => x.classList.toggle('is-on', x === c));
      subs.forEach((s) => (s.hidden = s.dataset.for !== c.dataset.cat));
    };
    c.addEventListener('mouseenter', activar);
    c.addEventListener('click', activar);
  });

  document.addEventListener('click', (e) => {
    const t = e.target as Node;
    if (mega && btn && !mega.contains(t) && !btn.contains(t)) abrir(false);
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') abrir(false);
  });
  mega?.querySelectorAll('a').forEach((a) => a.addEventListener('click', () => abrir(false)));

  const input = document.getElementById('tm-buscar-input') as HTMLInputElement | null;
  const qActual = new URLSearchParams(location.search).get('q');
  if (input && qActual) input.value = qActual;

  const badge = document.getElementById('tm-cart-count');
  let total = 0;

  document.addEventListener('click', (e) => {
    const el = e.target as HTMLElement;

    const step = el.closest<HTMLButtonElement>('.tm-qty__b');
    if (step) {
      const cont = step.closest('.tm-prod__qty');
      const n = cont?.querySelector<HTMLElement>('.tm-qty__n');
      if (n) {
        const val = Math.max(1, (parseInt(n.textContent || '1', 10) || 1) + Number(step.dataset.step));
        n.textContent = String(val);
      }
      return;
    }

    const add = el.closest<HTMLButtonElement>('.tm-prod__add');
    if (add) {
      const card = add.closest('.tm-prod');
      const n = card?.querySelector<HTMLElement>('.tm-qty__n');
      total += parseInt(n?.textContent || '1', 10) || 1;
      if (badge) {
        badge.textContent = String(total);
        badge.hidden = false;
      }
      const prev = add.textContent;
      add.textContent = 'Agregado!';
      add.classList.add('is-ok');
      window.setTimeout(() => {
        add.textContent = prev;
        add.classList.remove('is-ok');
      }, 1100);
    }
  });
</script>

<style>
  .tm-hdr {
    background: var(--tm-amarillo);
    position: sticky;
    top: 0;
    z-index: 80;
  }

  .tm-hdr__row {
    display: flex;
    align-items: center;
    gap: clamp(0.6rem, 2vw, 1.5rem);
    padding-block: 0.75rem;
  }

  .tm-logo {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    flex-shrink: 0;
  }

  .tm-logo__punto {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 50%;
    background: var(--tm-rojo);
  }

  .tm-logo__tx {
    font-weight: 800;
    font-size: 1.5rem;
    color: var(--tm-rojo);
    line-height: 1;
    font-style: italic;
  }

  .tm-logo__tx b {
    display: block;
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--tm-tinta);
    font-style: normal;
    margin-top: 0.1rem;
  }

  .tm-menu {
    position: relative;
    flex-shrink: 0;
  }

  .tm-menu__btn {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-weight: 700;
    font-size: 0.85rem;
    color: var(--tm-tinta);
    padding: 0.5rem 0.25rem;
  }

  .tm-menu__btn.is-active svg {
    transform: rotate(180deg);
  }

  .tm-menu__btn svg {
    transition: transform 0.15s ease;
  }

  .tm-buscar {
    flex: 1;
    max-width: 560px;
    display: flex;
    align-items: center;
    background: #fff;
    border-radius: var(--tm-pill);
    padding: 0.15rem 0.3rem 0.15rem 1rem;
  }

  .tm-buscar input {
    flex: 1;
    border: none;
    outline: none;
    background: none;
    padding: 0.55rem 0;
    font-size: 0.85rem;
  }

  .tm-buscar button {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: var(--tm-rojo);
    color: #fff;
    display: grid;
    place-items: center;
    flex-shrink: 0;
  }

  .tm-hdr__acc {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-shrink: 0;
  }

  .tm-hdr__ico {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.15rem;
    color: var(--tm-tinta);
    font-size: 0.62rem;
    font-weight: 600;
  }

  .tm-hdr__ico:hover {
    color: var(--tm-rojo);
  }

  .tm-hdr__badge[hidden] {
    display: none;
  }

  .tm-hdr__badge {
    position: absolute;
    top: -6px;
    right: -8px;
    min-width: 16px;
    height: 16px;
    padding: 0 4px;
    border-radius: var(--tm-pill);
    background: var(--tm-rojo);
    color: #fff;
    font-size: 0.6rem;
    font-weight: 700;
    display: grid;
    place-items: center;
  }

  .tm-mega {
    position: absolute;
    top: calc(100% + 0.75rem);
    left: 0;
    width: min(760px, 90vw);
    background: #fff;
    border-radius: 12px;
    box-shadow: 0 24px 48px -20px rgba(0, 0, 0, 0.35);
    display: grid;
    grid-template-columns: 230px 1fr;
    z-index: 90;
    overflow: hidden;
  }

  .tm-mega[hidden] {
    display: none;
  }

  .tm-mega__cats {
    list-style: none;
    background: var(--tm-suave);
    padding: 0.6rem;
  }

  .tm-mega__cat {
    width: 100%;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.55rem 0.6rem;
    border-radius: 8px;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--tm-tinta);
    text-align: left;
  }

  .tm-mega__ico {
    color: var(--tm-gris);
    flex-shrink: 0;
    display: grid;
    place-items: center;
  }

  .tm-mega__cat:hover,
  .tm-mega__cat.is-on {
    background: #fff;
    color: var(--tm-rojo);
    box-shadow: 0 2px 8px -4px rgba(0, 0, 0, 0.25);
  }

  .tm-mega__cat.is-on .tm-mega__ico,
  .tm-mega__cat:hover .tm-mega__ico {
    color: var(--tm-rojo);
  }

  .tm-mega__panel {
    padding: 1.25rem 1.5rem;
  }

  .tm-mega__sub h3 {
    font-size: 0.95rem;
    color: var(--tm-rojo);
    margin-bottom: 0.75rem;
  }

  .tm-mega__sub ul {
    list-style: none;
    columns: 2;
    column-gap: 1.5rem;
  }

  .tm-mega__sub li {
    margin-bottom: 0.6rem;
    break-inside: avoid;
  }

  .tm-mega__sub a {
    font-size: 0.82rem;
    color: var(--tm-gris);
  }

  .tm-mega__sub a:hover {
    color: var(--tm-rojo);
  }

  @media (max-width: 960px) {
    .tm-buscar {
      order: 3;
      flex-basis: 100%;
      max-width: none;
    }
    .tm-hdr__row {
      flex-wrap: wrap;
    }
    .tm-hdr__ico span {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .tm-mega {
      grid-template-columns: 1fr;
      width: 92vw;
    }
    .tm-mega__sub ul {
      columns: 1;
    }
  }
</style>
