<div class="wg-plus-top">
  <div class="wg-wrap wg-plus-top__inner">
    <span class="wg-plus-top__logo">tienda.com <b>Plus</b></span>
    <span class="wg-plus-top__txt">Aun no eres tienda.com Plus? Aprovecha el <b>DESPACHO GRATIS</b> en tus compras de supermercado desde S/ 79.90</span>
    <a href="#plus" class="wg-btn wg-btn--amarillo">Unete</a>
  </div>
</div>

<style>
  .wg-plus-top {
    background: var(--wg-negro);
    color: #fff;
  }

  .wg-plus-top__inner {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding-block: 0.55rem;
    flex-wrap: wrap;
  }

  .wg-plus-top__logo {
    font-weight: 800;
    font-size: 0.85rem;
    flex-shrink: 0;
  }

  .wg-plus-top__logo b {
    color: var(--wg-rosa);
  }

  .wg-plus-top__txt {
    font-size: 0.78rem;
    color: #d9d9d9;
    flex: 1;
  }

  .wg-plus-top__txt b {
    color: #fff;
  }

  @media (max-width: 760px) {
    .wg-plus-top__inner {
      justify-content: center;
      text-align: center;
    }
  }
</style>
