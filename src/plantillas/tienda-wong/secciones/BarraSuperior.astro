<div class="wg-top">
  <div class="wg-wrap wg-top__inner">
    <span>Como quieres recibir tu pedido?</span>
    <button type="button" class="wg-top__chip">Elegir</button>
  </div>
</div>

<style>
  .wg-top {
    background: #fff;
    border-bottom: 1px solid var(--wg-linea);
    font-size: 0.82rem;
  }

  .wg-top__inner {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding-block: 0.55rem;
  }

  .wg-top__chip {
    border: 1.5px solid var(--wg-rojo);
    color: var(--wg-rojo);
    font-weight: 700;
    font-size: 0.78rem;
    padding: 0.25rem 0.9rem;
    border-radius: 6px;
  }

  .wg-top__chip:hover {
    background: var(--wg-rojo);
    color: #fff;
  }
</style>
