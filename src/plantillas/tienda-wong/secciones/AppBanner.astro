---
import { getWongImg } from '../datos/wong.js';
---

<section class="wg-app">
  <div class="wg-wrap wg-app__inner">
    <div class="wg-app__tx">
      <p class="wg-app__kicker">Conoce los beneficios de nuestra</p>
      <h2>APP tienda.com</h2>
      <div class="wg-app__badges">
        <span class="wg-app__badge">Disponible en App Store</span>
        <span class="wg-app__badge">Disponible en Google Play</span>
      </div>
      <button type="button" class="wg-btn wg-btn--claro">Descargala aqui</button>
    </div>
    <img src={getWongImg('wg-app', 500, 360)} alt="App tienda.com Plus" loading="lazy" />
  </div>
</section>

<style>
  .wg-app {
    background: linear-gradient(135deg, var(--wg-amarillo) 0%, var(--wg-amarillo-osc) 100%);
  }

  .wg-app__inner {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 2rem;
    padding-block: 2.5rem;
  }

  .wg-app__kicker {
    font-size: 0.85rem;
    font-weight: 600;
  }

  .wg-app__tx h2 {
    font-size: clamp(1.5rem, 3.5vw, 2.2rem);
    color: #fff;
    margin: 0.25rem 0 1rem;
  }

  .wg-app__badges {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
    flex-wrap: wrap;
  }

  .wg-app__badge {
    background: #111;
    color: #fff;
    font-size: 0.72rem;
    font-weight: 700;
    padding: 0.5rem 0.9rem;
    border-radius: 8px;
  }

  .wg-app__inner img {
    border-radius: 16px;
    max-width: 260px;
  }

  @media (max-width: 700px) {
    .wg-app__inner {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .wg-app__badges {
      justify-content: center;
    }
    .wg-app__inner img {
      margin-inline: auto;
    }
  }
</style>
