---
import { marca } from '../datos/mecanico.js';
---

<div class="tm-chat" id="tm-chat">
  <button type="button" class="tm-chat__toggle" id="tm-chat-toggle" aria-label="Abrir chat de ayuda" aria-expanded="false">
    <svg class="tm-chat__i-open" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M21 15a4 4 0 0 1-4 4H8l-5 3V6a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z"></path>
    </svg>
    <span class="tm-chat__i-close" aria-hidden="true">&times;</span>
  </button>

  <section class="tm-chat__panel" id="tm-chat-panel" hidden aria-label="Chat de ayuda">
    <header class="tm-chat__head">
      <span class="tm-chat__ava" aria-hidden="true">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
          <path d="M14.5 6.5a3.5 3.5 0 0 1-4.6 4.6L5 16l3 3 4.9-4.9a3.5 3.5 0 0 1 4.6-4.6l-2.3 2.3-2-2z"></path>
        </svg>
      </span>
      <div>
        <b>Asistente {marca.nombre}</b>
        <small>En linea</small>
      </div>
    </header>

    <div class="tm-chat__body" id="tm-chat-body">
      <p class="tm-chat__msg tm-chat__msg--bot">Hola! Buscas equipos de diagnostico, boosters o herramientas para tu taller?</p>
      <p class="tm-chat__msg tm-chat__msg--bot">Cuentame que necesitas y te ayudo a encontrarlo.</p>
    </div>

    <form class="tm-chat__form" id="tm-chat-form">
      <input type="text" id="tm-chat-input" placeholder="Escribe aqui..." aria-label="Mensaje" autocomplete="off" />
      <button type="submit" aria-label="Enviar">&#10148;</button>
    </form>
    <p class="tm-chat__pow">Demo &mdash; respuestas automaticas</p>
  </section>
</div>

<script>
  import { chatCanned } from '../datos/mecanico.js';

  const toggle = document.getElementById('tm-chat-toggle');
  const panel = document.getElementById('tm-chat-panel');
  const body = document.getElementById('tm-chat-body');
  const form = document.getElementById('tm-chat-form');
  const input = document.getElementById('tm-chat-input') as HTMLInputElement | null;
  let paso = 0;

  function abrir(v: boolean) {
    if (!panel || !toggle) return;
    panel.hidden = !v;
    toggle.classList.toggle('is-open', v);
    toggle.setAttribute('aria-expanded', String(v));
    if (v) input?.focus();
  }

  toggle?.addEventListener('click', () => abrir(panel?.hidden ?? true));

  function push(texto: string, quien: 'bot' | 'yo') {
    if (!body) return;
    const p = document.createElement('p');
    p.className = `tm-chat__msg tm-chat__msg--${quien}`;
    p.textContent = texto;
    body.appendChild(p);
    body.scrollTop = body.scrollHeight;
  }

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    const txt = (input?.value || '').trim();
    if (!txt) return;
    push(txt, 'yo');
    if (input) input.value = '';
    const reply = chatCanned[Math.min(paso, chatCanned.length - 1)];
    paso++;
    window.setTimeout(() => push(reply, 'bot'), 650);
  });
</script>

<style>
  .tm-chat {
    position: fixed;
    right: clamp(1rem, 3vw, 2rem);
    bottom: clamp(1rem, 3vw, 2rem);
    z-index: 120;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.75rem;
  }

  .tm-chat__toggle {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--tm-naranja);
    color: #fff;
    display: grid;
    place-items: center;
    box-shadow: 0 12px 30px -10px rgba(244, 124, 32, 0.6);
    order: 2;
  }

  .tm-chat__i-close {
    display: none;
    font-size: 1.7rem;
    line-height: 1;
  }

  .tm-chat__toggle.is-open .tm-chat__i-open { display: none; }
  .tm-chat__toggle.is-open .tm-chat__i-close { display: block; }

  .tm-chat__panel[hidden] {
    display: none;
  }

  .tm-chat__panel {
    width: min(340px, 88vw);
    background: #fff;
    color: var(--tm-negro);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 24px 60px -16px rgba(0, 0, 0, 0.5);
    display: flex;
    flex-direction: column;
    order: 1;
  }

  .tm-chat__head {
    background: var(--tm-negro);
    color: #fff;
    padding: 0.9rem 1.1rem;
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .tm-chat__ava {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: var(--tm-naranja);
    display: grid;
    place-items: center;
    font-size: 1rem;
  }

  .tm-chat__head b {
    color: #fff;
    font-size: 0.9rem;
    display: block;
    font-family: var(--tm-font);
    text-transform: none;
  }

  .tm-chat__head small {
    font-size: 0.68rem;
    opacity: 0.75;
  }

  .tm-chat__body {
    background: #f4f4f4;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    max-height: 320px;
    min-height: 180px;
    overflow-y: auto;
  }

  .tm-chat__form {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.6rem;
    border-top: 1px solid #eee;
  }

  .tm-chat__form input {
    flex: 1;
    border: 1px solid #ddd;
    border-radius: 999px;
    padding: 0.55rem 0.9rem;
    font-size: 0.82rem;
    outline: none;
    color: var(--tm-negro);
  }

  .tm-chat__form input:focus {
    border-color: var(--tm-naranja);
  }

  .tm-chat__form button {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--tm-naranja);
    color: #fff;
    flex-shrink: 0;
  }

  .tm-chat__pow {
    text-align: center;
    font-size: 0.62rem;
    color: #999;
    padding: 0 0 0.6rem;
  }
</style>
