<section id="inicio" class="b-hero">
  <div class="b-contenedor">
    <h1 class="b-hero__titulo">Titular principal de la plantilla</h1>
    <p class="b-hero__texto">
      Subtitulo breve que explica la propuesta de valor en una o dos lineas.
    </p>
    <a href="#contacto" class="b-boton">Llamada a la accion</a>
  </div>
</section>

<style>
  .b-hero {
    background: linear-gradient(160deg, var(--b-tinta), #000);
    color: #fff;
    padding-block: clamp(4rem, 12vw, 8rem);
  }
  .b-hero__titulo {
    font-size: clamp(2rem, 6vw, 3.4rem);
    margin-bottom: 1rem;
    max-width: 20ch;
  }
  .b-hero__texto {
    max-width: 44ch;
    margin-bottom: 2rem;
    opacity: 0.85;
  }
</style>
