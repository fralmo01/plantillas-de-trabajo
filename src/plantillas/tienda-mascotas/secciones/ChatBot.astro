---
import { marca } from '../datos/mascotas.js';
import Icono from '../componentes/Icono.astro';
---

<div class="ms-chat" id="ms-chat">
  <button type="button" class="ms-chat__toggle" id="ms-chat-toggle" aria-label="Abrir opciones de ayuda" aria-expanded="false">
    <Icono name="chat" size={24} />
    <span class="ms-chat__i-close" aria-hidden="true">&times;</span>
  </button>

  <div class="ms-chat__fan" id="ms-chat-fan" hidden>
    <button type="button" class="ms-chat__opcion" id="ms-chat-whatsapp">
      <span class="ms-chat__opcion-ico ms-chat__opcion-ico--wsp"><Icono name="whatsapp" size={20} /></span>
      Escribenos por WhatsApp
    </button>
    <button type="button" class="ms-chat__opcion" id="ms-chat-abrir">
      <span class="ms-chat__opcion-ico"><Icono name="chat" size={18} /></span>
      Chatea con nosotros
    </button>
  </div>

  <section class="ms-chat__panel" id="ms-chat-panel" hidden aria-label="Chat de ayuda">
    <header class="ms-chat__head">
      <span class="ms-chat__ava" aria-hidden="true"><Icono name="pelota" size={16} /></span>
      <div>
        <b>Asistente {marca.nombre}</b>
        <small>En linea</small>
      </div>
      <button type="button" class="ms-chat__cerrar-panel" id="ms-chat-cerrar-panel" aria-label="Cerrar chat">
        <Icono name="cerrar" size={16} />
      </button>
    </header>

    <div class="ms-chat__body" id="ms-chat-body">
      <p class="ms-chat__msg ms-chat__msg--bot">¡Hola! ¿Buscas algo para tu perro, gato u otra mascota?</p>
      <p class="ms-chat__msg ms-chat__msg--bot">Cuentame que necesitas y te ayudo a encontrarlo.</p>
    </div>

    <form class="ms-chat__form" id="ms-chat-form">
      <input type="text" id="ms-chat-input" placeholder="Escribe aqui..." aria-label="Mensaje" autocomplete="off" />
      <button type="submit" aria-label="Enviar">&#10148;</button>
    </form>
    <p class="ms-chat__pow">Demo &mdash; respuestas automaticas</p>
  </section>
</div>

<script>
  import { chatCanned } from '../datos/mascotas.js';

  const toggle = document.getElementById('ms-chat-toggle');
  const fan = document.getElementById('ms-chat-fan');
  const panel = document.getElementById('ms-chat-panel');
  const abrirChatBtn = document.getElementById('ms-chat-abrir');
  const cerrarPanelBtn = document.getElementById('ms-chat-cerrar-panel');
  const whatsappBtn = document.getElementById('ms-chat-whatsapp');
  const body = document.getElementById('ms-chat-body');
  const form = document.getElementById('ms-chat-form');
  const input = document.getElementById('ms-chat-input') as HTMLInputElement | null;
  let paso = 0;

  function abrirFan(v: boolean) {
    if (!fan || !toggle) return;
    fan.hidden = !v;
    toggle.classList.toggle('is-open', v);
    toggle.setAttribute('aria-expanded', String(v));
  }

  toggle?.addEventListener('click', () => abrirFan(fan?.hidden ?? true));

  whatsappBtn?.addEventListener('click', () => {
    abrirFan(false);
  });

  abrirChatBtn?.addEventListener('click', () => {
    abrirFan(false);
    if (panel) panel.hidden = false;
    input?.focus();
  });

  cerrarPanelBtn?.addEventListener('click', () => {
    if (panel) panel.hidden = true;
  });

  function push(texto: string, quien: 'bot' | 'yo') {
    if (!body) return;
    const p = document.createElement('p');
    p.className = `ms-chat__msg ms-chat__msg--${quien}`;
    p.textContent = texto;
    body.appendChild(p);
    body.scrollTop = body.scrollHeight;
  }

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    const txt = (input?.value || '').trim();
    if (!txt) return;
    push(txt, 'yo');
    if (input) input.value = '';
    const reply = chatCanned[Math.min(paso, chatCanned.length - 1)];
    paso++;
    window.setTimeout(() => push(reply, 'bot'), 650);
  });
</script>

<style>
  .ms-chat {
    position: fixed;
    right: clamp(1rem, 3vw, 2rem);
    bottom: clamp(1rem, 3vw, 2rem);
    z-index: 120;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.75rem;
  }

  .ms-chat__toggle {
    width: 54px;
    height: 54px;
    border-radius: 50%;
    background: var(--ms-rojo);
    color: #fff;
    display: grid;
    place-items: center;
    box-shadow: 0 12px 30px -10px rgba(225, 6, 0, 0.6);
    order: 3;
  }

  .ms-chat__i-close {
    display: none;
    font-size: 1.6rem;
    line-height: 1;
  }

  .ms-chat__toggle.is-open :global(svg) { display: none; }
  .ms-chat__toggle.is-open .ms-chat__i-close { display: block; }

  .ms-chat__fan[hidden] {
    display: none;
  }

  .ms-chat__fan {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    order: 2;
  }

  .ms-chat__opcion {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    background: #fff;
    box-shadow: 0 12px 30px -14px rgba(0, 0, 0, 0.4);
    padding: 0.7rem 1rem;
    border-radius: 999px;
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--ms-texto);
    white-space: nowrap;
  }

  .ms-chat__opcion-ico {
    width: 26px;
    height: 26px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    background: var(--ms-rojo);
    color: #fff;
    flex-shrink: 0;
  }

  .ms-chat__opcion-ico--wsp {
    background: #25d366;
  }

  .ms-chat__panel[hidden] {
    display: none;
  }

  .ms-chat__panel {
    width: min(340px, 88vw);
    background: #fff;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 24px 60px -16px rgba(0, 0, 0, 0.45);
    display: flex;
    flex-direction: column;
    order: 1;
  }

  .ms-chat__head {
    background: var(--ms-negro);
    color: #fff;
    padding: 0.9rem 1.1rem;
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .ms-chat__ava {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--ms-rojo);
    display: grid;
    place-items: center;
    flex-shrink: 0;
  }

  .ms-chat__head > div {
    flex: 1;
  }

  .ms-chat__head b {
    color: #fff;
    font-size: 0.88rem;
    display: block;
  }

  .ms-chat__head small {
    font-size: 0.66rem;
    opacity: 0.75;
  }

  .ms-chat__cerrar-panel {
    color: #fff;
    opacity: 0.7;
  }

  .ms-chat__body {
    background: var(--ms-fondo);
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    max-height: 300px;
    min-height: 170px;
    overflow-y: auto;
  }

  .ms-chat__form {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.6rem;
    border-top: 1px solid var(--ms-linea);
  }

  .ms-chat__form input {
    flex: 1;
    border: 1px solid var(--ms-linea);
    border-radius: 999px;
    padding: 0.55rem 0.9rem;
    font-size: 0.8rem;
    outline: none;
  }

  .ms-chat__form input:focus {
    border-color: var(--ms-rojo);
  }

  .ms-chat__form button {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: var(--ms-rojo);
    color: #fff;
    flex-shrink: 0;
  }

  .ms-chat__pow {
    text-align: center;
    font-size: 0.6rem;
    color: var(--ms-gris-tenue);
    padding: 0 0 0.6rem;
  }
</style>
