<section class="tt-hero" id="inicio">
  <div class="tt-hero__fondo">
    <img 
      src="https://images.unsplash.com/photo-1552346154-21d32810aba3?q=80&w=1600&auto=format&fit=crop" 
      alt="Nueva Colección tu tienda.com Sports 2026" 
      class="tt-hero__imagen"
      loading="eager"
    />
    <div class="tt-hero__gradiente"></div>
  </div>

  <div class="tt-contenedor tt-hero__contenido">
    <div class="tt-hero__tag">
      <span class="tt-badge tt-badge--neon">NUEVA TEMPORADA 2026</span>
      <span class="tt-hero__serie">ORIGINALS & PERFORMANCE</span>
    </div>

    <h1 class="tt-hero__titulo">
      SPEED & STYLE<br />
      <span class="tt-hero__titulo--sub">ROMPE TUS PROPIOS LÍMITES</span>
    </h1>

    <p class="tt-hero__bajada">
      Diseñadas para dominar las calles y elevar tu rendimiento atlético. Descubre la máxima amortiguación responsiva y el confort supremo.
    </p>

    <div class="tt-hero__acciones">
      <a href="#calzado" class="tt-boton tt-boton--blanco">
        COMPRAR HOMBRE <span>→</span>
      </a>
      <a href="#calzado" class="tt-boton tt-boton--blanco">
        COMPRAR MUJER <span>→</span>
      </a>
      <a href="#outlet" class="tt-boton tt-boton--outline-blanco">
        OUTLET 40% OFF <span>→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .tt-hero {
    position: relative;
    min-height: min(85vh, 720px);
    display: flex;
    align-items: flex-end;
    padding-bottom: clamp(3rem, 6vw, 5rem);
    background: var(--tt-negro);
    color: var(--tt-blanco);
    overflow: hidden;
  }

  .tt-hero__fondo {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .tt-hero__imagen {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 35%;
    filter: brightness(0.82) contrast(1.1);
  }

  .tt-hero__gradiente {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.25) 0%,
      rgba(0, 0, 0, 0.4) 40%,
      rgba(0, 0, 0, 0.88) 100%
    );
  }

  .tt-hero__contenido {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 900px;
    margin-inline: auto 0;
  }

  .tt-hero__tag {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    margin-bottom: 1rem;
  }

  .tt-hero__serie {
    font-family: var(--tt-font-display);
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    color: var(--tt-blanco);
    opacity: 0.9;
  }

  .tt-hero__titulo {
    font-size: clamp(2.4rem, 6vw, 4.8rem);
    line-height: 0.95;
    margin-bottom: 1.2rem;
    text-shadow: 0 4px 16px rgba(0, 0, 0, 0.6);
  }

  .tt-hero__titulo--sub {
    color: var(--tt-amarillo-neon);
    font-weight: 800;
    font-size: clamp(1.6rem, 3.8vw, 3rem);
    letter-spacing: -0.01em;
  }

  .tt-hero__bajada {
    font-size: clamp(0.95rem, 1.3vw, 1.15rem);
    max-width: 600px;
    line-height: 1.5;
    margin-bottom: 2rem;
    color: rgba(255, 255, 255, 0.92);
  }

  .tt-hero__acciones {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
  }

  .tt-hero__acciones .tt-boton span {
    transition: transform 0.2s ease;
  }

  .tt-hero__acciones .tt-boton:hover span {
    transform: translateX(4px);
  }

  @media (max-width: 640px) {
    .tt-hero {
      min-height: 580px;
      padding-bottom: 2.5rem;
    }
    .tt-hero__acciones {
      flex-direction: column;
      width: 100%;
    }
    .tt-hero__acciones .tt-boton {
      width: 100%;
      text-align: center;
    }
  }
</style>
