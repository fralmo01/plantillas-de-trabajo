<section class="tt-cinematic" id="adizero">
  <div class="tt-cinematic__img-wrap">
    <img 
      src="https://images.unsplash.com/photo-1517838277536-f5f99be501cd?q=80&w=1600&auto=format&fit=crop" 
      alt="Adizero Dropset Pro Tu Tienda" 
      loading="lazy"
    />
    
    
    <button class="tt-cinematic__btn-pausa" aria-label="Pausar animación">
      <span class="tt-barra-pausa"></span>
      <span class="tt-barra-pausa"></span>
    </button>

    
    <div class="tt-cinematic__caja">
      <div class="tt-cinematic__badge-titulo">
        ADIZERO DROPSET PRO
      </div>
      <div class="tt-cinematic__badge-sub">
        Entrena híbrido
      </div>
      <a href="#calzado" class="tt-btn-caja tt-btn-caja--blanco tt-cinematic__btn">
        Comprar ahora <span>→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .tt-cinematic {
    position: relative;
    background: var(--tt-negro);
    width: 100%;
    overflow: hidden;
  }

  .tt-cinematic__img-wrap {
    position: relative;
    width: 100%;
    min-height: clamp(400px, 60vh, 680px);
  }

  .tt-cinematic__img-wrap img {
    width: 100%;
    height: 100%;
    min-height: clamp(400px, 60vh, 680px);
    object-fit: cover;
    object-position: center 40%;
    display: block;
  }

  .tt-cinematic__btn-pausa {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background: transparent;
    border: none;
    display: flex;
    gap: 4px;
    cursor: pointer;
    padding: 0.4rem;
    z-index: 5;
  }

  .tt-barra-pausa {
    width: 3px;
    height: 16px;
    background: var(--tt-blanco);
    display: block;
  }

  
  .tt-cinematic__caja {
    position: absolute;
    bottom: clamp(2rem, 5vw, 4rem);
    left: clamp(1.5rem, 5vw, 4rem);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.25rem;
    z-index: 5;
  }

  .tt-cinematic__badge-titulo {
    background: var(--tt-negro);
    color: var(--tt-blanco);
    padding: 0.45rem 0.9rem;
    font-family: var(--tt-font-condensed);
    font-size: clamp(1rem, 2vw, 1.4rem);
    font-weight: 900;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .tt-cinematic__badge-sub {
    background: var(--tt-blanco);
    color: var(--tt-negro);
    padding: 0.35rem 0.8rem;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .tt-cinematic__btn {
    margin-top: 0.4rem;
    font-size: 0.82rem;
    padding: 0.65rem 1.4rem;
  }

  .tt-cinematic__btn span {
    transition: transform 0.2s ease;
  }

  .tt-cinematic__btn:hover span {
    transform: translateX(4px);
  }
</style>
