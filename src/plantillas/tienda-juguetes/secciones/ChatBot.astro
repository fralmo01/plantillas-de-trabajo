---
import { marca } from '../datos/juguetes.js';
---

<div class="tj-chat" id="tj-chat">
  <button type="button" class="tj-chat__toggle" id="tj-chat-toggle" aria-label="Abrir chat de ayuda" aria-expanded="false">
    <svg class="tj-chat__i-open" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M21 15a4 4 0 0 1-4 4H8l-5 3V6a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z"></path>
    </svg>
    <span class="tj-chat__i-close" aria-hidden="true">&times;</span>
  </button>

  <section class="tj-chat__panel" id="tj-chat-panel" hidden aria-label="Chat de ayuda">
    <header class="tj-chat__head">
      <span class="tj-chat__ava" aria-hidden="true">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="9" width="7" height="7" rx="1.5"></rect>
          <rect x="14" y="9" width="7" height="7" rx="1.5"></rect>
          <path d="M6.5 9V6a1.5 1.5 0 0 1 3 0v3M17.5 9V6a1.5 1.5 0 0 0-3 0v3"></path>
        </svg>
      </span>
      <div>
        <b>Asistente {marca.nombre}</b>
        <small>En linea</small>
      </div>
    </header>

    <div class="tj-chat__body" id="tj-chat-body">
      <p class="tj-chat__msg tj-chat__msg--bot">Hola! Buscas un juguete para alguna edad u ocasion especial?</p>
      <p class="tj-chat__msg tj-chat__msg--bot">Cuentame que necesitas y te ayudo a elegir.</p>
    </div>

    <form class="tj-chat__form" id="tj-chat-form">
      <input type="text" id="tj-chat-input" placeholder="Escribe aqui..." aria-label="Mensaje" autocomplete="off" />
      <button type="submit" aria-label="Enviar">&#10148;</button>
    </form>
    <p class="tj-chat__pow">Demo &mdash; respuestas automaticas</p>
  </section>
</div>

<script>
  import { chatCanned } from '../datos/juguetes.js';

  const toggle = document.getElementById('tj-chat-toggle');
  const panel = document.getElementById('tj-chat-panel');
  const body = document.getElementById('tj-chat-body');
  const form = document.getElementById('tj-chat-form');
  const input = document.getElementById('tj-chat-input') as HTMLInputElement | null;
  let paso = 0;

  function abrir(v: boolean) {
    if (!panel || !toggle) return;
    panel.hidden = !v;
    toggle.classList.toggle('is-open', v);
    toggle.setAttribute('aria-expanded', String(v));
    if (v) input?.focus();
  }

  toggle?.addEventListener('click', () => abrir(panel?.hidden ?? true));

  function push(texto: string, quien: 'bot' | 'yo') {
    if (!body) return;
    const p = document.createElement('p');
    p.className = `tj-chat__msg tj-chat__msg--${quien}`;
    p.textContent = texto;
    body.appendChild(p);
    body.scrollTop = body.scrollHeight;
  }

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    const txt = (input?.value || '').trim();
    if (!txt) return;
    push(txt, 'yo');
    if (input) input.value = '';
    const reply = chatCanned[Math.min(paso, chatCanned.length - 1)];
    paso++;
    window.setTimeout(() => push(reply, 'bot'), 650);
  });
</script>

<style>
  .tj-chat {
    position: fixed;
    right: clamp(1rem, 3vw, 2rem);
    bottom: clamp(1rem, 3vw, 2rem);
    z-index: 120;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.75rem;
  }

  .tj-chat__toggle {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--tj-verde);
    color: #fff;
    display: grid;
    place-items: center;
    box-shadow: 0 12px 30px -10px rgba(79, 138, 104, 0.6);
    order: 2;
  }

  .tj-chat__i-close {
    display: none;
    font-size: 1.7rem;
    line-height: 1;
  }

  .tj-chat__toggle.is-open .tj-chat__i-open { display: none; }
  .tj-chat__toggle.is-open .tj-chat__i-close { display: block; }

  .tj-chat__panel[hidden] {
    display: none;
  }

  .tj-chat__panel {
    width: min(340px, 88vw);
    background: #fff;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 24px 60px -16px rgba(0, 0, 0, 0.35);
    display: flex;
    flex-direction: column;
    order: 1;
  }

  .tj-chat__head {
    background: var(--tj-verde);
    color: #fff;
    padding: 0.9rem 1.1rem;
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .tj-chat__ava {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    display: grid;
    place-items: center;
    font-size: 1.1rem;
  }

  .tj-chat__head b {
    color: #fff;
    font-size: 0.9rem;
    display: block;
  }

  .tj-chat__head small {
    font-size: 0.7rem;
    opacity: 0.85;
  }

  .tj-chat__body {
    background: var(--tj-crema);
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    max-height: 320px;
    min-height: 180px;
    overflow-y: auto;
  }

  .tj-chat__form {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.6rem;
    border-top: 1px solid var(--tj-linea);
  }

  .tj-chat__form input {
    flex: 1;
    border: 1px solid var(--tj-linea);
    border-radius: var(--tj-pill);
    padding: 0.55rem 0.9rem;
    font-size: 0.82rem;
    outline: none;
  }

  .tj-chat__form input:focus {
    border-color: var(--tj-verde);
  }

  .tj-chat__form button {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--tj-verde);
    color: #fff;
    flex-shrink: 0;
  }

  .tj-chat__pow {
    text-align: center;
    font-size: 0.62rem;
    color: var(--tj-gris);
    padding: 0 0 0.6rem;
  }
</style>
